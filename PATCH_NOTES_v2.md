# PATCH NOTES v2 - 교재 공통재고/차수별 출고 흐름 보정

## 반영 목적
- 기존 재고 수량(stock_quantity, released_quantity)은 변경하지 않고 화면/업무 로직만 수정했습니다.
- 교재는 과정 그룹별 공통 재고로 입고되고, 출고는 주차/차수별로 처리되는 업무 구조를 반영했습니다.
- 관리자/담당자/매니저 권한 차단을 제거하고, 작업자 선택 여부만 확인하도록 유지했습니다.

## 주요 수정
1. 공통재고 기반 출고대기 상태 계산 추가
   - DB 상태가 `입고대기`인 차수라도 같은 과정 그룹에 가용 재고가 있으면 화면상 `출고대기`로 표시합니다.
   - DB 상태를 강제로 변경하지 않습니다.

2. 출고 메뉴 흐름 보정
   - 입출고 화면에서 `자회사_신입직원` 같은 과정 그룹에 입고를 하면, 같은 그룹의 해당 주차/차수가 출고 메뉴에서 바로 `출고대기` 및 `출고 처리` 대상으로 표시됩니다.
   - 가용재고 계산은 `그룹 총 입고 - 그룹 총 출고` 기준입니다.

3. 과정조회/대시보드/관리자 화면 표시 보정
   - 공통재고가 있는 미출고 차수는 `입고대기`가 아니라 `출고대기` 흐름으로 보이게 했습니다.
   - 상태 필터도 실제 DB 상태뿐 아니라 계산된 업무상태를 함께 반영합니다.

4. 출고 기록 보강
   - 출고 처리 시 `actual_release_date`도 함께 저장하도록 보정했습니다.

## 변경하지 않은 항목
- 기존 DB의 재고 수량은 변경하지 않았습니다.
- 기존 입고/출고 이력 로그는 변경하지 않았습니다.
- 기존 사용자 역할명은 유지하되, 기능 권한 차단에는 사용하지 않습니다.

## 테스트 권장 순서
1. 입고 탭에서 특정 과정 그룹에 입고 처리
2. 출고 탭에서 해당 주차 선택
3. 같은 과정 그룹의 차수가 `출고대기`로 표시되는지 확인
4. 출고 처리 후 이력 탭에 로그가 남는지 확인
5. 잔여재고가 `총 입고 - 총 출고`로 맞는지 확인
