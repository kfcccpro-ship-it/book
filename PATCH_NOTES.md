# 교재관리 시스템 수정 설계 및 반영 내역

## 반영 방향
- 기존 DB에 기록된 재고 수량은 변경하지 않음.
- 권한은 관리자/담당자/매니저를 구분하지 않고, 작업자 선택 여부만 확인하도록 단순화.
- 입고는 과정 그룹(교재명/과정명) 단위의 공통 재고로 누적.
- 출고는 주차별·차수별 과정 단위로 처리.
- 그룹 잔여재고가 있으면 해당 차수의 개별 상태가 `입고대기`여도 출고 가능하도록 수정.

## 주요 오류 수정
1. `if (!currentUser != null)` 조건 오류 수정
   - 기존 조건은 작업자를 선택해도 항상 차단되는 형태였음.
   - `if (!currentUser)` 기준으로 수정.

2. 입출고 화면의 강제 출고 목록 수정
   - 기존: `입고완료` 상태 과정만 표시.
   - 수정: 같은 그룹의 잔여재고가 있으면 `출고완료` 이전 상태의 과정은 모두 출고 후보로 표시.

3. 출고 이력 기록 보정
   - 강제 출고 시 이전 상태를 무조건 `입고완료`로 기록하지 않고 실제 과정 상태를 기록하도록 수정.

4. 그룹 재고 기준 보강
   - 출고 수량 수정과 추가출고 화면에서도 개별 과정 재고가 아니라 그룹 가용재고 기준으로 계산하도록 보정.

## 운영상 의미
- 예: `여신기본` 교재 100권 입고 → `여신기본 1차` 40권 출고 → 몇 주 후 `여신기본 2차` 30권 출고 → 중간에 100권 추가 입고 가능.
- 이때 입고는 그룹 재고를 증가시키고, 출고는 각 차수의 `released_quantity`와 상태를 갱신함.
- 모든 작업은 work_logs에 남으므로 역할별 버튼 차단은 제거함.
